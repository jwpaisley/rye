/= recursive.rye =/
  def recursive(var x) {
      if (x > 0) {
          print("x = " + x);
          recursive(x - 1);
      } else {
          print("Recursion has completed.");
      }
  }
  recursive(10);
end
